# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build outputs
dist
dist-ssr
*.local

# Frozen snapshots generated by freeze.py (regenerated fresh on every CI run)
public/data.json
public/scenario.json
data/walk_forward_NOMACRO_test.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
env.bak/
venv.bak/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Data files and models (if large)
*.pkl
*.h5
*.model
*.bin
models/

# Per-contract runtime state (regenerated by the pipeline on every run).
# Tracked data/ files are evidence artifacts only: walk_forward_backtest_latest.json,
# macro_leakage_test.json, signal_state.json, eia_wti_spot_daily.json.
data/CL*_*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# MacOS
.DS_Store
.AppleDouble
.LSOverride

# Temporary files
*.tmp
*.temp
temp_repo/

# Claude Code local config
.claude/

# Interview-prep notes — personal, not for the public repo
GRILL_NOTES.md
