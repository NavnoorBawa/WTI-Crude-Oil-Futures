# Core Flask web server dependencies
Flask==3.0.0
Flask-CORS==4.0.0
gunicorn==21.2.0

# Core data processing - Python 3.13 compatible versions
numpy==1.26.4

# Financial data sources
yfinance==0.2.18
requests==2.31.0

# Basic dependencies that work with Python 3.13
python-dateutil==2.8.2
urllib3==2.2.0
packaging==25.0

# Remove problematic packages that cause compilation issues
# pandas, scikit-learn, xgboost, lightgbm, numba - not needed for basic operation
# The app works perfectly with numpy and yfinance only